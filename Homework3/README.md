# 项目说明文档

## 如何运行项目



1. **环境准备**

* 确保安装了对应开发环境（如 Node.js/Python/Java 等，根据实际项目技术栈补充）

* 安装依赖包：`[具体安装命令，如npm install/pip install]`

1. **配置项目**

* 复制配置文件模板：`cp config.example.json config.json`

* 根据实际环境修改配置文件中的数据库连接、端口等信息

1. **启动项目**

* 开发环境：`[开发启动命令，如npm run dev]`

* 生产环境：`[生产启动命令，如npm start]`

1. **验证运行**

* 项目启动后，访问 `http://localhost``:[端口号]` 确认服务正常运行

## API 接口列表

### 课程管理 (Course)



| 接口功能       | 请求方法   | 接口路径              | 说明             |
| ---------- | ------ | ----------------- | -------------- |
| 创建课程       | POST   | /api/courses      | 创建新的课程信息       |
| 查询所有课程     | GET    | /api/courses      | 获取系统中所有课程列表    |
| 根据 ID 查询课程 | GET    | /api/course/{id}  | 通过课程 ID 获取课程详情 |
| 更新课程       | PUT    | /api/courses/{id} | 更新指定 ID 的课程信息  |
| 删除课程       | DELETE | /api/courses/{id} | 删除指定 ID 的课程    |

### 学生管理 (Student)



| 接口功能       | 请求方法   | 接口路径               | 说明             |
| ---------- | ------ | ------------------ | -------------- |
| 创建学生       | POST   | /api/students      | 创建新的学生信息       |
| 查询所有学生     | GET    | /api/students      | 获取系统中所有学生列表    |
| 根据 ID 查询学生 | GET    | /api/students/{id} | 通过学生 ID 获取学生详情 |
| 更新学生       | PUT    | /api/students/{id} | 更新指定 ID 的学生信息  |
| 删除学生       | DELETE | /api/students/{id} | 删除指定 ID 的学生    |

### 选课管理 (Enrollment)



| 接口功能     | 请求方法   | 接口路径                  | 说明          |
| -------- | ------ | --------------------- | ----------- |
| 选课       | POST   | /api/enrollments      | 学生选择课程      |
| 查询所有选课记录 | GET    | /api/enrollments      | 获取系统中所有选课记录 |
| 退课       | DELETE | /api/enrollments/{id} | 学生退选课程      |

## 测试说明



1. **接口测试工具**

* 推荐使用 Postman、curl 或其他 API 测试工具

* 接口文档详情参考 `test-api.md` 文件

1. **测试步骤**

* 按照依赖顺序测试：先创建课程和学生，再进行选课操作

* 每个接口测试前确保服务已正常启动

* 测试数据可参考接口文档中的示例请求参数

1. **测试用例示例**

* 创建课程：使用 POST /api/courses，携带示例中的课程信息 JSON

* 创建学生：使用 POST /api/students，携带示例中的学生信息 JSON

* 选课操作：使用 POST /api/enrollments，关联已创建的课程 ID 和学生 ID

1. **响应验证**

* 验证响应状态码是否为 200（成功）

* 验证返回数据结构是否符合接口文档定义

* 对于查询接口，验证返回数据与创建 / 更新的数据一致

1. **注意事项**

* 测试删除和更新接口时，确保使用已存在的 ID

* 选课操作需确保课程和学生已存在且课程未达容量上限

> （注：文档部分内容可能由 AI 生成）